/**
 * Re-export the real DashboardPage as DashboardOverviewPage
 * so the route import in dashboard.routes.ts resolves correctly.
 */
export { DashboardPage as DashboardOverviewPage } from './dashboard.page';
